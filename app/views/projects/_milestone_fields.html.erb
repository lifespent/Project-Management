<fieldset>
	<%= f.label :title, "Milestone Title" %> 
	<%= f.text_field :title %>
	<%= link_to "Remove Milestone", '#', class: "remove_fields" %> <br />
	<%= f.fields_for :tasks do |builder| %>
	    <%= render 'task_fields', f: builder %>
	<% end %>
	  <%= link_to_add_fields "Add Task", f, :tasks %>
		<%= f.hidden_field :_destroy %>
</fieldset>